# Backend-port v2-main → main (productie) — ronde 2, 2026-06-14

**Status: KLAAR OM TE DEPLOYEN — THIERRY-GATED.** Niet automatisch uitgevoerd:
productie-deploy naar `api.aaavatar.nl` en DB-migraties tegen de live database
zijn handmatige, door Thierry te bevestigen acties. Dit bestand is de
review-bare voorbereiding.

## Wat moet er live
De cloud-routes die deze marathon op `v2-main` zijn geland (E09.2 / E11.2 /
E10.4 / E10.3 / E15.6 / E15.5) draaien nog niet op productie. De macOS-app
test ze tot die tijd tegen een Vercel-preview (zie onderaan).

## Methode: GECUREERDE port, GEEN merge
`git diff main..v2-main -- backend/` bevat divergentie die NIET mee mag:
- `backend/api/v1/auth/send-recovery-email.ts` staat **alleen op main** (toegevoegd
  bij de port van 2026-06-12 uit ongecommit v1-werk). Een blinde
  `git checkout v2-main -- backend/` zou dit bestand **verwijderen** → NIET doen.

Port daarom alléén de onderstaande bestanden byte-voor-byte van `v2-main` naar
een verse branch vanaf `main`:

| Bestand | Wijziging |
|---|---|
| `backend/api/v1/stylize.ts` | NIEUW productie-endpoint: `style` (4 stijlen) + `hair_preset`/`hair_prompt` + `clothes_preset`/`clothes_prompt` (server-gemapte intents), credit-gate (4 cr), `generation_model` (nano/OpenAI), dev `model_override`/vrij `prompt` |
| `backend/api/v1/upscale.ts` | NIEUW endpoint "Boost resolution" (Real-ESRGAN, 1 cr, credit-gate als colorize) |
| `backend/lib/models.ts` | `stylize` prod-registry, `USER_SELECTABLE_MODELS` + `resolveGenerationModel` (E15.6), `upscale` CloudFeature (E10.3), `fill_body` credits 1→2 (E14.3) |
| `backend/lib/replicate.ts` | `stylizeEdit` + `upscale` (+ STYLIZE_TIMEOUT) |
| `backend/vercel.json` | functie-config `api/v1/upscale.ts` (maxDuration 90) |
| `backend/api/v1/account.ts` | `is_dev_unlimited` in de dev-tak (E15.5) |
| `backend/scripts/models-smoke.ts` | smoke uitgebreid |

**NIET porten:** verwijdering van `send-recovery-email.ts` (op main houden).

## DB-migratie (apart, GATED tegen de live database)
- `backend/sql/013_newsletter_cohorts_revoke_public.sql` (+ check `012_device_grants_account_link_source.sql`,
  los punt uit E13.0). Controleer of 013 al op productie is toegepast (kwam mogelijk
  via de v1-fix-branch `fix/newsletter-cohorts-revoke-public`); zo niet, draai 'm
  handmatig na review. De cloud-endpoints hierboven hebben deze migratie NIET nodig
  om te functioneren.

## Env (productie)
- `REPLICATE_API_TOKEN` (bestaat al; nieuwe modellen draaien allemaal via Replicate:
  google/nano-banana, openai/gpt-image-1.5, black-forest-labs/flux-2-pro,
  nightmareai/real-esrgan — geen nieuwe secrets nodig).
- **Replicate-saldo bewaken** vóór go-live (onder $5 → 6/min-throttle; $0 → 402/500).
- Gepinde model-versies verifiëren op replicate.com bij een 404 (birefnet/deoldify/
  real-esrgan zijn gepind).

## Deploy-stappen (door Thierry)
1. `git checkout -b v1/backend-port-2026-06-14 main`
2. Kopieer de 7 bestanden hierboven uit `v2-main` (byte-identiek; diff verifiëren).
3. `cd backend && npm run typecheck` → groen.
4. (optioneel) `node`/`tsx backend/scripts/models-smoke.ts`.
5. ff-merge naar `main`, push → Vercel-productie-deploy (`avatars-api`, rootDirectory=backend).
6. DB: 013 indien nodig handmatig draaien.
7. Prod-smoke: `/v1/stylize` (style) zonder auth → 401; `/v1/upscale` zonder auth → 401;
   `/v1/account` → 200.

## Preview (huidig, voor e2e-verificatie vooraf)
- **https://avatars-r5jafqkdn-square-one-69d6814b.vercel.app** (project
  `square-one-69d6814b/avatars-api`, vers gedeployd 2026-06-14 vanaf v2-main).
- E2e-verificatie van het harde kleding-acceptatiecriterium loopt via **E01.15**
  (DEBUG backend-override + preview) — zelf wacht-op-Thierry (preview-protection-
  secret + dev-account-sessie). Doe die e2e bij voorkeur vóór de productie-deploy.
