# E27.1 kickoff — Canvas-camera (zoom + pan)

Plak het blok hieronder in een **verse Claude-sessie** (of zeg simpelweg: "Lees
`plan/E27-27.1-kickoff.md` en bouw 27.1 volgens die instructie").

---

Bouw E27.1 — Canvas-camera (zoom + pan). Strikte spelregels, kwaliteit boven snelheid.

## Context / setup
- Werk in de v2-worktree: `/Users/thierry/Documents/Dev Projects/Avatars/.claude/worktrees/feat-e04-4.1`
- Sync eerst: checkout `v2-main` (lokaal-only, geen remote-push), bevestig schoon.
- Lees VÓÓR alles: `plan/E27-canvas-viewport.md` → de 27.1-aanpak staat daar volledig uitgeschreven
  (stap 1-6). Volg dat plan. Lees ook `plan/E24-canvas-ia.md` stories 24.31 + 24.32 (die mag je NIET
  regresseren).
- Branch per story: `v2/E27-27.1`, merge `--no-ff` naar `v2-main`. Done = NÁ merge.

## Spelregels (strikt)
claim (status in_progress + owner in het plan, in de claim-commit) → Plan → bouwen → DoD: beide
targets groen via `./scripts/build-v2.sh` (Avatar v1 + Avatar2 + alle pakkettests) → MERGE naar
v2-main → Result-regel + status done in het plan. Elke UI-story: visuele smoke + screenshot.
Figma-afwijkingen onder "Figma-TODO:".

## Kernregel (bewaken)
VIEW-zoom (camera: scale+offset over de HELE scène) ≠ ONDERWERP schalen (de afbeelding-in-frame via
selectie-handles — dat blijft E24). Dit vervangt de mislukte view-zoom uit 24.8/24.17.

## Relevante code
- `Avatar2/Features/Editor/EditorView.swift` — `canvasViewZoom`/`canvasMaxViewZoom` (verwijderen),
  `editorBody` met DSCanvasCard (camera-transform hierop: `.scaleEffect(anchor:.center).offset()`),
  `hasBackground`, de Original-branch (24.31, moet onder zoom blijven werken).
- `Avatar2/Features/Editor/EditorCanvasView.swift` — `viewZoom`/`magnifyGesture`/`applyViewZoom`
  (vervangen door de camera), handle-positie-math (~r357-361, gebruikt nu viewZoom → terug naar 1×),
  en de 24.32-gesture-structuur: subject-drag op de `Image`, deselect-tap op `Color.clear`, ESC-knop.
  DEZE MOGEN NIET BREKEN.
- DEBUG smoke-haken: args worden gelezen in `ShellView.swift` (`if args.contains("--…")`), debug-
  functies in `ShellModel.swift` (`#if DEBUG`). Voeg bv. `--cam-zoom <factor>` / `--cam-fit` toe.

## Bouwvolgorde (uit het plan — incrementeel, tussentijds smoken)
1. `CanvasCamera` (struct: scale 0.25–4.0 geclampt, offset) + scène-transform op DSCanvasCard +
   pinch (MagnificationGesture) + ⌘+/⌘−/⌘0(fit)/⌘1(100%) via verborgen keyboardShortcut-knoppen.
   Pure SwiftUI, laag risico. → BUILD + smoke + screenshot per zoomniveau (50/100/200/400%).
2. Pas dan: NSViewRepresentable event-catcher voor scroll(=pan)/⌘-scroll+magnify(=zoom)/spatie-drag,
   als achtergrondlaag die clicks DOORLAAT (`hitTest`→nil voor muis-down). Zoom-rond-cursor.
3. REGRESSIETEST verplicht: 24.32 (klik-buiten-na-drag deselect + ESC) en 24.31 (Original-modus)
   werken nog op elk zoomniveau. Smoke + screenshot als bewijs.

## Niet doen
Geen live DB-migraties, geen productie-deploys, geen v1 (`Avatar/`) aanraken.

## Verificatie-notes (uit ervaring)
- `./scripts/build-v2.sh` eindigt met "alles groen" als alles klopt. SourceKit "No such module
  AvatarKit/AvatarUI"-diagnostics zijn transiënte indexering — negeren, de xcodebuild is leidend.
- Screenshot: launch de app, `osascript -e 'tell application "Aaavatar 2" to activate'`, dan
  `screencapture -x`. De window-capture is gedegradeerd — een terminal kan het beeld overlappen;
  breng Aaavatar expliciet naar voren en herhaal indien nodig.

Eindig met eindrapport + Figma-TODO.
